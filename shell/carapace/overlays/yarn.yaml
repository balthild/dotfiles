# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: yarn
completion:
  positionalany:
    - "$carapace.tools.npm.Scripts"
    - "$carapace.tools.yarn.Bins"

commands:
  - name: run
    completion:
      positionalany:
        - "$carapace.tools.npm.Scripts"
        - "$carapace.tools.yarn.Bins"
