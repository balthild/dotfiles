# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: yarn
description: Node.js package manager.
completion:
  positionalany:
    - "$carapace.tools.npm.Scripts"
    - "$carapace.tools.yarn.Bins"

commands:
  - name: run
    description: Run a script or executable.
    completion:
      positionalany:
        - "$carapace.tools.npm.Scripts"
        - "$carapace.tools.yarn.Bins"
